version: '3.8'

services:
  web:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data  # Persist data
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
